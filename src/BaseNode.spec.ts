/* eslint-env mocha */

import { assert } from 'chai'

describe('BaseNode', () => {
  it('should work', () => {
    assert.strictEqual(true, true, 'should be true')
  })

  it('should work', () => {
    assert.strictEqual(true, true, 'should be true')
  })
})
